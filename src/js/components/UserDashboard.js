import React from 'react';
import Collection from './Collection';

const UserDashboard = () => (
  <div id="app">
    <div id="container">
      <Collection />
    </div>
  </div>
);

export default UserDashboard;
